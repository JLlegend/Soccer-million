// Firestore Rules: paste these into Firebase Console → Firestore Database → Rules.
// This starter keeps the child page public and requires Firebase Authentication
// for writing. For a family version, sign the parent in anonymously or with email,
// then replace PARENT_UID below with that account's UID.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /challenge/main {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == '9gQZGi6ne8Wlw83iUB1WnUnD2Bo2';
    }
    match /submissions/{submissionId} {
      allow read: if request.auth != null && request.auth.uid == '9gQZGi6ne8Wlw83iUB1WnUnD2Bo2';
      allow create: if request.resource.data.status == 'pending'
                    && request.resource.data.shots is int
                    && request.resource.data.shots > 0
                    && request.resource.data.shots <= 100000;
      allow update, delete: if request.auth != null && request.auth.uid == '9gQZGi6ne8Wlw83iUB1WnUnD2Bo2';
    }
    match /goals/{goalId} {
      allow read: if true;
      allow create: if request.resource.data.text is string
                    && request.resource.data.text.size() > 0
                    && request.resource.data.text.size() <= 180;
      allow update, delete: if request.auth != null && request.auth.uid == '9gQZGi6ne8Wlw83iUB1WnUnD2Bo2';
    }
  }
}
